# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Prevent package-lock.json generation (using yarn)
package-lock=false

# Enable exact versions for dependencies
save-exact=true

# Security settings
audit-level=moderate

# Publishing settings
//registry.npmjs.org/:_authToken=${NPM_TOKEN}